import { recipe as basic } from './basic';
import { recipe as positions } from './positions';
import { recipe as sizes } from './sizes';
import { recipe as withTrigger } from './with-trigger';

export const drawerRecipes = [basic, positions, sizes, withTrigger];
