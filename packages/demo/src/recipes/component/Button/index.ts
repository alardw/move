import { recipe as basic } from './basic';
import { recipe as variants } from './variants';
import { recipe as sizes } from './sizes';
import { recipe as withIcon } from './with-icon';
import { recipe as disabled } from './disabled';

export const buttonRecipes = [basic, variants, sizes, withIcon, disabled];
