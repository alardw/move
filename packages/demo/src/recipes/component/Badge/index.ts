import { recipe as basic } from './basic';
import { recipe as colors } from './colors';
import { recipe as dot } from './dot';
import { recipe as sizes } from './sizes';
import { recipe as variants } from './variants';

export const badgeRecipes = [basic, variants, colors, sizes, dot];
