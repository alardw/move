import { recipe as basic } from './basic';
import { recipe as withAvatar } from './with-avatar';
import { recipe as metaShorthand } from './meta-shorthand';
import { recipe as sizes } from './sizes';
import { recipe as density } from './density';
import { recipe as lineClamp } from './line-clamp';
import { recipe as interactive } from './interactive';
import { recipe as withAction } from './with-action';
import { recipe as withSeparator } from './with-separator';

export const listRecipes = [basic, withAvatar, metaShorthand, withAction, sizes, density, lineClamp, interactive, withSeparator];
