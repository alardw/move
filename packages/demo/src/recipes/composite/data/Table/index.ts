import { recipe as basic } from './basic';
import { recipe as sortable } from './sortable';
import { recipe as withPagination } from './with-pagination';

export const tableRecipes = [basic, sortable, withPagination];
