import { recipe as basic } from './basic';
import { recipe as forgotPassword } from './forgot-password';
import { recipe as resetPassword } from './reset-password';

export const loginRecipes = [basic, forgotPassword, resetPassword];
