# Copy to .env.local and paste a free key from https://api.nasa.gov (instant, 1000/hr).
# Without it the app falls back to DEMO_KEY, which is rate-limited (and shows the
# feed's error state when exhausted). .env.local is gitignored (*.local).
VITE_NASA_API_KEY=
