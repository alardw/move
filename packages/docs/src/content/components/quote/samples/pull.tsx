import { Quote } from 'move';

export default function PullSample() {
  return (
    <Quote variant="pull" attribution="Claude, Opus 4.8">
      The value I invent feels identical to the value I look up — same confidence, no inner sense
      that one of them is a guess.
    </Quote>
  );
}
