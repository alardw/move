# Dependencies
node_modules/

# Build output
dist/
release/
*.tsbuildinfo

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local
.env.*.local
.aider*

# AI tooling — local only. The skills themselves live in packages/move/skills;
# .agents/.claude are convenience pointers recreated per machine (and consumers
# get the skills via `npx move skills`). Kept out of git so the repo doesn't
# ship fragile symlinks that break on Windows / show up empty on GitHub.
.agents/
.claude/

# Vitest browser-mode auto-captures screenshots on test failure
# (screenshotFailures). These are machine-specific run artifacts, not committed
# baselines — our browser tests assert DOM invariants, not visual snapshots.
**/__screenshots__/
